# Change Summary

- TID: 566
- CHANGE_PREFIX: 20260331-1230

**User Prompt:**
Objective:add test case by pytest

- create tests folder for pytest test cases
- study current code base and update design docs
- implement test cases for encoder and decoder

**Task information:
- TID: 566
- CHANGE_PREFIX: 20260331-1230
** Additional Instructions:** (ver 1.3.0)
...

**Original Branch Commit ID:**
1d094ea47704cdf6315ff5cdd6fd56200c6e0009

**Root Cause:**
No test framework or automated test cases were implemented for the existing `qr_encoder_lite.py` and `qr_decoder_lite.py` codebases. Furthermore, the `.gitignore` was not completely ignoring test-related cache artifacts.

**Solution:**
1. Created a `tests` directory and implemented `pytest`-based automated unit tests in `tests/test_qr_encoder_lite.py` and `tests/test_qr_decoder_lite.py`.
2. Mocked external dependencies like `subprocess.run` (for ffmpeg) and `zxingcpp.read_barcodes` in `test_qr_decoder_lite.py`.
3. Tested internal components in `qr_encoder_lite.py` such as interleaving, RS encoding, payload size limits, and format/version info computation.
4. Updated `qr_decoder.md` and `qr_encoder.md` documentation files with instructions on running tests.
5. Adjusted `.gitignore` and removed any generated `__pycache__` artifacts.