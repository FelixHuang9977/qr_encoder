# Task Summary
- TID: 567
- CHANGE_PREFIX: 20260331-1440

## User Prompt
Objective:add makefile

support below command:
* make setup   #setup python venv
* make test       #run pytest
* make help      #show command list and help in makefile

## Original Branch Commit ID
f5a62ddac770c15151c715b24a9721a6a0bda670

## Root Cause
Missing Makefile to simplify development workflow (setup, test, help).

## Solution
Added `Makefile` with targets `setup`, `test`, and `help`. `setup` sets up a Python virtual environment and installs dependencies, `test` runs `pytest`, and `help` displays the available commands.
